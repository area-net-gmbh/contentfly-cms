general:
  branches:
    only:
      - master
deployment:
  dev:
    branch: master
    commands:
      - ant -lib apache-commons-net.jar deploy-dev
  release:
    tag: /.*/
    commands:
      - ant -lib apache-commons-net.jar deploy-release