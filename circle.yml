general:
  branches:
    only:
      - master
deployment:
  dev:
    branch: master
    commands:
      - ant -lib ~/bin/apache-commons-net.jar deploy-dev
  release:
    tag: /.*/
    commands:
      - ant -lib ~/bin/apache-commons-net.jar deploy-release